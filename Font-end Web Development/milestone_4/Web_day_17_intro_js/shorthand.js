// variable declaration
var tomatoPrice = 25;
// variable value update

tomatoPrice = 10;

// add something to the previsous value

var price1 = 31;
var price2 = 10;

// set a new value
// price1 =35;

// add some value to the existhing value

price1 = price1 + 10;
// console.log(price1);

// addition to the same variable shorthand
// += means adding some value to the existhing value

price1 += 10;

price2 = price2 - 5;
// shorthand -=
// -= means deducting some values from the current value of the verint
price2 -= 5;
